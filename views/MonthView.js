import React, { Component } from 'react';
import {
    AppRegistry,
    PropTypes,
    StyleSheet,
    Text,
    View
    } from 'react-native';

export default class MonthView extends Component {
    render() {
        return <View><Text>Month View</Text></View>
    }
}

MonthView.propTypes = {
};

const styles = StyleSheet.create({

});
