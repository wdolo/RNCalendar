import React, { Component } from 'react';
import {
    AppRegistry,
    PropTypes,
    StyleSheet,
    Text,
    View
    } from 'react-native';

export default class WeekView extends Component {
    render() {
        return <View><Text>Week View</Text></View>
    }
}

WeekView.propTypes = {
};

const styles = StyleSheet.create({

});
